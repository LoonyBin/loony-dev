[project]
name = "loony-dev"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["click>=8.0"]

[project.scripts]
loony-dev = "loony_dev.cli:cli"

[tool.setuptools.packages.find]
include = ["loony_dev*"]
