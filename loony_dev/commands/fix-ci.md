---
description: Fix failing CI checks on a PR
argument-hint: <PR number/title, branch, and failing check details>
---
Fix CI failures on a PR.

$ARGUMENTS

Instructions:
- Review the CI logs at the URLs above
- Identify the root cause of each failure
- Make targeted fixes on the PR branch
- Do not change unrelated code
- Push the fixes when done
